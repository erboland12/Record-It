<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.recordratings">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".credentials.ProfileActivity"
            android:theme="@style/MainTheme"></activity>
        <activity android:name=".MyRecordsActivity" />
        <activity android:name=".credentials.SignInActivity" />
        <activity android:name=".credentials.LoginActivity" />
        <activity android:name=".settings.PopupRatingActivity" />
        <activity android:name=".settings.PopupFeedbackActivity" />
        <activity android:name=".records.EditRecord" />
        <activity android:name=".settings.SettingsActivity" />
        <activity
            android:name=".records.RecordsPage"
            android:theme="@style/MainTheme" />
        <activity android:name=".records.AddRecord" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/MainTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.SEND_SMS" />

</manifest>